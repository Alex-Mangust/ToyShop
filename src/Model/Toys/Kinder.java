package Model.Toys;

/** Класс, описывающий поведение игрушки Kinder. Является наследником класса Toy */
public class Kinder extends Toy {

    /**
     * Конструктор класса
     * @param name - имя игрушки
     */
    public Kinder(String name) {
        super(name);
    }
}
